{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "attach",
      "name": "Attach to Chrome",
      "port": 9357,
      "url": "http://localhost:4200*",
      "webRoot": "${workspaceFolder}",
      "sourceMapPathOverrides": {
        "lisweb-frontend/*": "${workspaceFolder}/app/*"
      }
    }
  ]
}
