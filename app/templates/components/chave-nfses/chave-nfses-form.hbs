{{#if @model}}
<div class="form-horizontal">
  <div class="form-group">
    <label class="col-sm-2 control-label">
      Chave:
    </label>
    <div class="col-sm-6">
      <Input @type="text" @value={{@model.chave}} class="form-control" placeholder="Chave" autofocus/>
    </div>
  </div>
  <div class="form-group">
    <label class="col-sm-2 control-label">
      Valor:
    </label>
    <div class="col-sm-6">
      <Input @type="text" @value={{@model.valor}} class="form-control" placeholder="Valor" autofocus/>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-success" {{on "click" (fn this.save @model)}}>
        Salvar
      </button>
    </div>
  </div>
</div>
{{else}}
Regsitro não Encontrado
{{/if}}
