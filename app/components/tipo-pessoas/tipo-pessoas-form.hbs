{{#with (changeset model this.tipospessoa) as |changesetObj|}}
  <PowerSelect
    @changeset={{changesetObj}}
    @submit={{this.submit}}
    @rollback={{this.rollback}}
  >
    {{changesetObj}}
  </PowerSelect>
{{/with}}
