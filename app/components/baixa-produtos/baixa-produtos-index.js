import { tracked } from '@glimmer/tracking';
import DefaultComponent from '../default/default';

export default class BaixaProdutosIndexComponent extends DefaultComponent {
  @tracked modelString = 'baixa_produto';
}
