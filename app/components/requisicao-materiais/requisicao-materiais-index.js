import { tracked } from '@glimmer/tracking';
import DefaultComponent from '../default/default';

export default class RequisicaoMateriaisIndexComponent extends DefaultComponent {
  @tracked modelString = 'requisicao_material';
}
