<div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <LinkTo @route="chave_nfses/new" class="btn btn-success">Nova chave nfse</LinkTo>
    </div>
  </div>
</div>
<div class="form-group">
  <div class="col-sm-offset-2 col-sm-10">
    <label for="order">Ordem:</label>
    <select id ="order" {{on "change" (fn this.sortData ) }}>
      <option value='chave'>Chave cresc</option>
      <option value='-chave'>Chave decresc</option>
      <option value='valor'>Valor cresc</option>
      <option value='-valor'>Valor decresc</option>
      <option value='id'>Id cresc</option>
      <option value='-id'>Id decresc</option>
    </select>
  </div>
</div>
<table class="table table-stripped">
  <thead>
    <th>Id</th>
    <th>Chave</th>
    <th>Valor</th>
    <th colspan="2"></th>
  </thead>
  <tbody>
    {{#each this.model as |chave_nfse|}}
      <tr>
        <td>{{chave_nfse.id}}</td>
        <td>{{chave_nfse.chave}}</td>
        <td>{{chave_nfse.valor}}</td>
        <td>
          <LinkTo @route="chave_nfses/edit" @model={{chave_nfse}} class="btn btn-primary">Editar</LinkTo>
          <button type="button" class="btn btn-primary" {{on "click" (fn this.delete chave_nfse)}}>Excluir</button>
        </td>
      </tr>
    {{else}}
      <tr><td collspan="5">{{this.loading}}</td></tr>
    {{/each}}
  </tbody>
</table>
<button type="button" class="btn btn-success" {{on "click" this.pageDown}}>Anterior</button>
<button type="button" class="btn btn-success" {{on "click" this.pageUp}}>Próxima</button>
