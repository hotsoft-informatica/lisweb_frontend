import { tracked } from '@glimmer/tracking';
import DefaultComponent from '../default/default';

export default class GrupoCentroCustosIndexComponent extends DefaultComponent {
  @tracked modelString = 'grupo_centro_custo';
}
